import { PureComponent, ReactNode } from "react";
import "./SpinnerLoader.scss";

class SpinnerLoader extends PureComponent {
  render(): ReactNode {
    return <div className="spinner-loader" />;
  }
}

export default SpinnerLoader;
